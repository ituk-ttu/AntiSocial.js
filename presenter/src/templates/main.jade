.container-fluid.questions-container
    .row
        .col-lg-9.questions
            .question(ng-repeat="question in questions.slice().reverse() track by $index"
                    ng-animate="{enter: 'animate-enter', leave: 'animate-leave'}")
                p {{question}}
        .col-lg-3
            .timer
                p {{time}}